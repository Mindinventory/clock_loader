name: clock_loader
description: A clock loader which you can use to display a smooth and great loading effect.
version: 0.0.3
homepage: https://github.com/mi-ghanshyam/clock_loader

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
